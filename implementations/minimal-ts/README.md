# Minimal independent implementation

This package independently implements only the v1 Transition ID and state-root
algorithms. It does not import `@erc-awar/core`; its test consumes the shared
Golden Vector.

This is dependency independence inside one repository. It is not a substitute
for adoption and maintenance by an external team.
