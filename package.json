{
  "name": "erc-awar",
  "version": "0.0.0",
  "private": true,
  "description": "ERC-83xx: Agent Experience Delta and Verifiable Memory Commitment — monorepo.",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
