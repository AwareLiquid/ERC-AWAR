{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://erc-awar.dev/schemas/memory-categories/embedding.schema.json",
  "title": "MEMORY_EMBEDDING content",
  "description": "Dense vector memory. The schema commits to the model and vector dimensionality so consumers never treat incompatible vectors as interchangeable.",
  "type": "object",
  "required": ["model", "dim"],
  "properties": {
    "model": { "type": "string", "examples": ["all-MiniLM-L6-v2"] },
    "dim": { "type": "integer", "minimum": 1, "examples": [384] },
    "metric": { "enum": ["cosine", "dot", "l2"] },
    "dtype": { "enum": ["float32", "float16", "int8"] },
    "vectorUri": {
      "type": "string",
      "description": "Private off-chain locator for raw vector bytes; only its commitment is public."
    }
  },
  "additionalProperties": true
}
