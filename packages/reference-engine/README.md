# @erc-awar/reference-engine

Full off-chain reference state machine for Agent Memory State v1.

It prepares privacy-preserving transitions, maintains a local state accumulator,
accepts externally prepared Deltas, and verifies the complete sequence/root
history. Application-level diff and merge helpers remain separate from the
normative linear protocol.
